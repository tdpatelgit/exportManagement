[pytest]
# Run from the crm_app/ directory so `import app` / `import config` resolve.
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q
filterwarnings =
    ignore::DeprecationWarning
